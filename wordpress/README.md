# WordPress

## Run 

Copiar las variables de entorno de ejemplo.

```bash
cp .env.wordpress.EXAMPLE .env.wordpress
cp .env.mysql.EXAMPLE .env.mysql
```

Cambiar los valores necesarios y luego ejecutar.

```sh
docker compose up -d
```
